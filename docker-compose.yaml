version: '3.8'

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: nestjs
      MYSQL_USER: nestjs
      MYSQL_PASSWORD: nestjs
      MYSQL_ROOT_PASSWORD: nestjs
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
